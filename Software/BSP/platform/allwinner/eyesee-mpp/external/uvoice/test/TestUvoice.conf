########### paramter (ref to tulip_cedarx.conf)############
[parameter]
save_dir_path = "/mnt/extsd/TestUvoice_Files"
save_file_name = "TestUvoice.wav"
pcm_sample_rate = 16000
pcm_channel_cnt = 1
pcm_bit_width = 16
pcm_cap_duration = 0 #unit:s, 0:infinite
uvoice_data_interval = 160 #unit:ms, it means the size of data which is sent to uvoicelib once.

#if add wav header to save as a wav file which can be played directly.
save_wav = 0